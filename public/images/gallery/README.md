# Фото для галереи

Положите сюда кадры работ и мастерской. Имена файлов должны совпадать с полем
`image` в `src/data/gallery.js` (`g1.jpg` … `g8.jpg`).

Можно использовать вертикальные и горизонтальные кадры — раскладка masonry
подстроится через поле `span` (`tall` / `wide` / `normal`).
